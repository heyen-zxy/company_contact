<%= error_messages_for :user%>
<div class="row">
  <div class="col-sm-5">
    <%= form_for @user, url: user_path do |f| %>
        <%= render partial: 'users/form', locals: {f: f} %>

    <% end %>
  </div>
  <div class="col-sm-4">
  </div>
</div>